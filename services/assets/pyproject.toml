[project]
name = "assets"
version = "0.1.0"
description = "OpenMarket v2 — Assets & Images (Python FastAPI)"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "sqlalchemy[asyncio]",
    "asyncpg",
    "pillow",
    "pydantic",
]
